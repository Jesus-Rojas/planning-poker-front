import { DecimalToCommaPipe } from './decimal-to-comma.pipe';

describe('DecimalToCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalToCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
